import styled from "styled-components";

const UpperCircleStyle = styled.div`
  @media only screen and (max-width: 375px) {
    
  }
  position: absolute;
  z-index: 0;
`;

export default UpperCircleStyle;
